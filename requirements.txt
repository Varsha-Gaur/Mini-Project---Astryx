# SecureGrid — Hybrid HE + DP Smart Grid Aggregation
# Core
numpy>=1.24.0
pandas>=2.0.0
# Dashboard
streamlit>=1.32.0
plotly>=5.20.0
# API server (optional)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.5.0
# Homomorphic encryption (optional — MockHE used if absent)
# tenseal>=0.3.14
# Differential privacy validated backend (optional — numpy fallback if absent)
# diffprivlib>=0.6.4
# HTTP client
httpx>=0.27.0